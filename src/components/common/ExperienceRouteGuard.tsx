/**
 * DEPRECATED — neutralized. SystemStateGuard is the single orchestrator
 * for navigation. This component is kept only to avoid breaking imports
 * and is now a no-op.
 */
export default function ExperienceRouteGuard() {
  return null;
}
