# Sprint K — Testes Forenses do NOS\n\n**Data Planejada:** 24-30 de Maio de 2026  \n**Status:** 📋 PLANEJAMENTO  \n**Objetivo:** Criar suite de testes automatizados para validar invariantes do NOS\n\n---\n\n## Visão Geral\n\nSprint K implementa testes forenses para garantir que o NOS mantém suas invariantes críticas:\n\n1. **Snapshots são imutáveis** após publicação\n2. **Cálculos são determinísticos** (mesma entrada = mesma saída)\n3. **Isolamento de tenant** é respeitado\n4. **Performance de índices** é mantida\n5. **Boundaries arquiteturais** são respeitadas\n\n---\n\n## Tarefas\n\n### Tarefa K1: Testes de Integridade de Snapshots\n\n**Arquivo:** `src/features/nos/__tests__/snapshot-integrity.test.ts`\n\n**Testes:**\n\n```typescript\n// Teste 1: Snapshot não pode ser modificado após publicação\ntest('snapshot is immutable after publish', async () => {\n  // 1. Criar plano\n  // 2. Publicar plano (snapshot congelado)\n  // 3. Tentar modificar snapshot\n  // 4. Verificar que modificação falha\n});\n\n// Teste 2: Snapshot contém todos os dados necessários\ntest('snapshot contains all required fields', async () => {\n  // 1. Criar plano com alimentos, receitas, combos\n  // 2. Publicar plano\n  // 3. Verificar que snapshot contém:\n  //    - meals com items\n  //    - macros congelados\n  //    - version_history\n  //    - source_recipe_version\n});\n\n// Teste 3: Snapshot não referencia dados externos\ntest('snapshot is self-contained', async () => {\n  // 1. Criar plano\n  // 2. Publicar plano\n  // 3. Deletar alimentos/receitas/combos originais\n  // 4. Verificar que snapshot ainda funciona\n});\n\n// Teste 4: Snapshot versionado corretamente\ntest('snapshot versioning is correct', async () => {\n  // 1. Criar plano v1\n  // 2. Publicar plano v1\n  // 3. Editar plano → v2\n  // 4. Publicar plano v2\n  // 5. Verificar que v1 e v2 são diferentes\n  // 6. Verificar que v1 não foi modificado\n});\n```\n\n**Critério de Sucesso:**\n- ✅ 4/4 testes passam\n- ✅ Coverage > 90%\n\n---\n\n### Tarefa K2: Testes de Determinismo de Cálculos\n\n**Arquivo:** `src/features/nos/engine/__tests__/calc-determinism.test.ts`\n\n**Testes:**\n\n```typescript\n// Teste 1: calcMacros é determinístico\ntest('calcMacros is deterministic', () => {\n  const food = { kcal_100g: 100, protein_100g: 10, ... };\n  const qty = 150;\n  \n  const result1 = calcMacros(food, qty);\n  const result2 = calcMacros(food, qty);\n  \n  expect(result1).toEqual(result2);\n});\n\n// Teste 2: calcRecipeTotals é determinístico\ntest('calcRecipeTotals is deterministic', () => {\n  const ingredients = [...];\n  \n  const result1 = calcRecipeTotals(ingredients);\n  const result2 = calcRecipeTotals(ingredients);\n  \n  expect(result1).toEqual(result2);\n});\n\n// Teste 3: Ordem de ingredientes não afeta totais\ntest('ingredient order does not affect totals', () => {\n  const ingredients = [food1, food2, food3];\n  const shuffled = [food3, food1, food2];\n  \n  const result1 = calcRecipeTotals(ingredients);\n  const result2 = calcRecipeTotals(shuffled);\n  \n  expect(result1.kcal).toBe(result2.kcal);\n  expect(result1.protein).toBe(result2.protein);\n});\n\n// Teste 4: Arredondamento é consistente\ntest('rounding is consistent', () => {\n  // Testar valores que causam arredondamento\n  const food = { kcal_100g: 123.456, ... };\n  const qty = 33.333;\n  \n  const result = calcMacros(food, qty);\n  \n  // Verificar que resultado tem exatamente 2 casas decimais\n  expect(result.kcal).toBe(Math.round(result.kcal * 100) / 100);\n});\n\n// Teste 5: Cálculos são idempotentes\ntest('calculations are idempotent', () => {\n  const food = { kcal_100g: 100, ... };\n  const qty = 150;\n  \n  const result1 = calcMacros(food, qty);\n  const result2 = calcMacros(result1, qty); // Aplicar novamente\n  \n  // Resultado não deve mudar\n  expect(result2).toEqual(result1);\n});\n```\n\n**Critério de Sucesso:**\n- ✅ 5/5 testes passam\n- ✅ Coverage > 95%\n\n---\n\n### Tarefa K3: Testes de Isolamento de Tenant\n\n**Arquivo:** `src/features/nos/__tests__/tenant-isolation.test.ts`\n\n**Testes:**\n\n```typescript\n// Teste 1: Tenant A não vê dados de Tenant B\ntest('tenant isolation: cannot see other tenant data', async () => {\n  // 1. Criar alimento em Tenant A\n  // 2. Criar alimento em Tenant B\n  // 3. Query como Tenant A\n  // 4. Verificar que vê apenas alimento de A\n});\n\n// Teste 2: RLS bloqueia acesso cruzado\ntest('rls blocks cross-tenant access', async () => {\n  // 1. Criar receita em Tenant A\n  // 2. Tentar acessar como Tenant B\n  // 3. Verificar que acesso é negado\n});\n\n// Teste 3: Dados globais são visíveis para todos\ntest('global data is visible to all tenants', async () => {\n  // 1. Criar alimento global (tenant_id = NULL)\n  // 2. Query como Tenant A\n  // 3. Query como Tenant B\n  // 4. Verificar que ambos veem alimento global\n});\n\n// Teste 4: Custom data é isolado por tenant\ntest('custom data is isolated by tenant', async () => {\n  // 1. Criar alimento custom em Tenant A\n  // 2. Query como Tenant B\n  // 3. Verificar que Tenant B não vê alimento custom de A\n});\n\n// Teste 5: Nutricionista não vê dados de outro nutricionista\ntest('nutritionist isolation within tenant', async () => {\n  // 1. Criar receita por Nutricionista A\n  // 2. Criar receita por Nutricionista B\n  // 3. Query como Nutricionista A\n  // 4. Verificar que vê apenas receita de A\n});\n```\n\n**Critério de Sucesso:**\n- ✅ 5/5 testes passam\n- ✅ RLS validado\n- ✅ Coverage > 90%\n\n---\n\n### Tarefa K4: Testes de Performance de Índices\n\n**Arquivo:** `src/features/nos/__tests__/index-performance.test.ts`\n\n**Testes:**\n\n```typescript\n// Teste 1: Busca por source_priority é rápida\ntest('search by source_priority is fast', async () => {\n  // 1. Inserir 10k alimentos\n  // 2. Query com filtro source_priority\n  // 3. Verificar que tempo < 100ms\n});\n\n// Teste 2: Busca por tenant é rápida\ntest('search by tenant is fast', async () => {\n  // 1. Inserir 10k alimentos em múltiplos tenants\n  // 2. Query com filtro tenant_id\n  // 3. Verificar que tempo < 100ms\n});\n\n// Teste 3: Busca por categoria é rápida\ntest('search by category is fast', async () => {\n  // 1. Inserir 10k alimentos com categorias\n  // 2. Query com filtro category\n  // 3. Verificar que tempo < 100ms\n});\n\n// Teste 4: Listagem de receitas do nutricionista é rápida\ntest('nutritionist recipe list is fast', async () => {\n  // 1. Inserir 1k receitas\n  // 2. Query com filtro nutritionist_id\n  // 3. Verificar que tempo < 100ms\n});\n\n// Teste 5: Deduplicação por hash é rápida\ntest('deduplication by hash is fast', async () => {\n  // 1. Inserir 10k alimentos com hashes\n  // 2. Query com filtro canonical_hash\n  // 3. Verificar que tempo < 100ms\n});\n```\n\n**Critério de Sucesso:**\n- ✅ 5/5 testes passam\n- ✅ Todos os tempos < 100ms\n- ✅ Índices estão sendo usados (EXPLAIN ANALYZE)\n\n---\n\n### Tarefa K5: Testes de Boundaries Arquiteturais\n\n**Arquivo:** `src/features/nos/__tests__/boundaries.test.ts`\n\n**Testes:**\n\n```typescript\n// Teste 1: calcEngine não importa Supabase\ntest('calcEngine has no Supabase imports', () => {\n  const source = fs.readFileSync('src/features/nos/engine/calcEngine.ts', 'utf-8');\n  expect(source).not.toMatch(/from ['\"@\\/lib\\/supabase/);\n  expect(source).not.toMatch(/from ['\"@supabase/);\n});\n\n// Teste 2: calcEngine não importa componentes\ntest('calcEngine has no component imports', () => {\n  const source = fs.readFileSync('src/features/nos/engine/calcEngine.ts', 'utf-8');\n  expect(source).not.toMatch(/from ['\"@\\/components/);\n  expect(source).not.toMatch(/from ['\"@\\/features/);\n});\n\n// Teste 3: Patient App não importa NOS\ntest('patient app does not import NOS', () => {\n  const patientFiles = glob.sync('src/pages/PatientMealPlan.tsx');\n  patientFiles.forEach(file => {\n    const source = fs.readFileSync(file, 'utf-8');\n    expect(source).not.toMatch(/from ['\"@\\/features\\/nos/);\n  });\n});\n\n// Teste 4: Patient App não importa calcEngine\ntest('patient app does not import calcEngine', () => {\n  const patientFiles = glob.sync('src/components/patient/**/*.tsx');\n  patientFiles.forEach(file => {\n    const source = fs.readFileSync(file, 'utf-8');\n    expect(source).not.toMatch(/calcEngine/);\n  });\n});\n\n// Teste 5: ESLint boundaries são respeitadas\ntest('eslint boundaries are enforced', async () => {\n  // Executar eslint com .eslintrc.nos-boundaries.json\n  // Verificar que não há violações\n});\n```\n\n**Critério de Sucesso:**\n- ✅ 5/5 testes passam\n- ✅ Boundaries são respeitadas\n- ✅ ESLint não reporta violações\n\n---\n\n### Tarefa K6: Testes de Regressão End-to-End\n\n**Arquivo:** `e2e/nos-regression.spec.ts`\n\n**Testes:**\n\n```typescript\n// Teste 1: Fluxo completo de criação de plano\ntest('complete meal plan creation flow', async ({ page }) => {\n  // 1. Login como nutricionista\n  // 2. Ir para Editor V3\n  // 3. Buscar alimento (NOSFoodSearch)\n  // 4. Criar receita (RecipeBuilder)\n  // 5. Criar combo (ComboBuilder)\n  // 6. Adicionar combo ao plano\n  // 7. Publicar plano\n  // 8. Verificar que snapshot foi criado\n});\n\n// Teste 2: Fluxo de paciente visualizando plano\ntest('patient viewing published plan', async ({ page }) => {\n  // 1. Login como paciente\n  // 2. Visualizar plano publicado\n  // 3. Verificar que macros estão corretos\n  // 4. Fazer substituição\n  // 5. Verificar que macros não mudaram (snapshot)\n});\n\n// Teste 3: Fluxo de edição de plano\ntest('meal plan editing flow', async ({ page }) => {\n  // 1. Login como nutricionista\n  // 2. Editar plano publicado\n  // 3. Criar nova versão\n  // 4. Publicar nova versão\n  // 5. Verificar que versão antiga não foi modificada\n});\n\n// Teste 4: Fluxo de reutilização de combos\ntest('combo reuse flow', async ({ page }) => {\n  // 1. Login como nutricionista\n  // 2. Criar combo\n  // 3. Salvar combo\n  // 4. Criar novo plano\n  // 5. Reutilizar combo\n  // 6. Verificar que macros estão corretos\n});\n\n// Teste 5: Fluxo de biblioteca pessoal\ntest('personal library flow', async ({ page }) => {\n  // 1. Login como nutricionista\n  // 2. Adicionar alimento à biblioteca\n  // 3. Adicionar receita à biblioteca\n  // 4. Adicionar combo à biblioteca\n  // 5. Verificar que biblioteca está acessível\n  // 6. Usar item da biblioteca\n});\n```\n\n**Critério de Sucesso:**\n- ✅ 5/5 testes passam\n- ✅ Sem regressões\n- ✅ Performance aceitável\n\n---\n\n## Estrutura de Testes\n\n```\nsrc/features/nos/\n├── __tests__/\n│   ├── snapshot-integrity.test.ts\n│   ├── calc-determinism.test.ts\n│   ├── tenant-isolation.test.ts\n│   ├── index-performance.test.ts\n│   └── boundaries.test.ts\n├── engine/\n│   ├── calcEngine.ts\n│   └── __tests__/\n│       └── calc-determinism.test.ts\n└── ...\n\ne2e/\n└── nos-regression.spec.ts\n```\n\n---\n\n## Configuração de Testes\n\n### Vitest Config\n\n```typescript\n// vitest.config.ts\nexport default defineConfig({\n  test: {\n    globals: true,\n    environment: 'node',\n    coverage: {\n      provider: 'v8',\n      reporter: ['text', 'json', 'html'],\n      include: ['src/features/nos/**/*.ts'],\n      exclude: ['**/*.test.ts', '**/__tests__/**'],\n      lines: 90,\n      functions: 90,\n      branches: 85,\n      statements: 90,\n    },\n  },\n});\n```\n\n### Playwright Config\n\n```typescript\n// playwright.config.ts\nexport default defineConfig({\n  testDir: './e2e',\n  fullyParallel: true,\n  forbidOnly: !!process.env.CI,\n  retries: process.env.CI ? 2 : 0,\n  workers: process.env.CI ? 1 : undefined,\n  reporter: 'html',\n  use: {\n    baseURL: 'http://localhost:5173',\n    trace: 'on-first-retry',\n  },\n});\n```\n\n---\n\n## Execução de Testes\n\n```bash\n# Testes unitários\nnpm run test -- src/features/nos/__tests__/\n\n# Testes com coverage\nnpm run test -- --coverage src/features/nos/\n\n# Testes E2E\nnpm run test:e2e -- e2e/nos-regression.spec.ts\n\n# Todos os testes\nnpm run test:all\n```\n\n---\n\n## Critérios de Sucesso\n\n### ✅ Testes Unitários\n\n- [ ] 25/25 testes passam\n- [ ] Coverage > 90%\n- [ ] Sem warnings\n\n### ✅ Testes E2E\n\n- [ ] 5/5 testes passam\n- [ ] Sem regressões\n- [ ] Performance aceitável\n\n### ✅ Boundaries\n\n- [ ] ESLint não reporta violações\n- [ ] Imports são respeitados\n- [ ] RLS é validado\n\n---\n\n## Próximos Passos\n\n1. **Sprint L — Integração com Cockpit**\n   - Usar combos salvos como templates rápidos\n   - Histórico de combos usados\n   - Sugestões baseadas em uso\n\n2. **Sprint M — Mobile Optimization**\n   - Interface touch-friendly\n   - Busca otimizada para mobile\n   - Sincronização offline\n\n3. **Sprint N — Analytics e Insights**\n   - Rastrear uso de alimentos/receitas/combos\n   - Sugerir alimentos mais usados\n   - Análise de padrões nutricionais\n\n---\n\n## Conclusão\n\nSprint K implementa testes forenses para garantir que o NOS mantém suas invariantes críticas. Após Sprint K, o sistema estará pronto para integração com Cockpit e otimizações de mobile.\n\n**Status:** 📋 PRONTO PARA IMPLEMENTAÇÃO\n