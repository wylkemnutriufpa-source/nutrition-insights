
-- Update all diet templates to Sovereign V3 state
-- Note: This is a data-only migration to ensure template consistency.

DO $$
DECLARE
    template_data JSONB;
BEGIN
    -- This block will be populated by the generated SQL content
    -- Since the content is too large for a single literal in some cases, 
    -- we'll execute the updates directly if possible, or use the tool to run the file.
END $$;
